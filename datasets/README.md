# Datasets

Public neuromorphic sets (MNIST-DVS, SHD, N-MNIST) will live here.

**Task 3 default**: synthetic spike patterns generated in
`sw/examples/keyword_spotting_train.py` (no download). For snnTorch + real SHD later,
add download scripts under this folder and keep large archives out of git.
